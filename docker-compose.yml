services:
  frontend:
    image: ${DOCKER_USER}/digitalfix-frontend:latest
    container_name: digitalfix-frontend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf
      - ./certs:/etc/nginx/certs
    restart: always